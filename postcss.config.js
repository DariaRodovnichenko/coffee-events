export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

// This code is a PostCSS configuration file that uses the Tailwind CSS plugin and autoprefixer.
// It exports a default object with a plugins property that includes the Tailwind CSS plugin and an empty autoprefixer configuration.
